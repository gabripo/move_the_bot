FROM node:22-slim

RUN npm install -g openclaw@latest

WORKDIR /app
COPY package.json tsconfig.json ./
RUN npm install

COPY src/ ./src/
COPY skills/ ./skills/

RUN npm run build

ENV OPENCLAW_MODEL=ollama/llama3.2
ENV OLLAMA_BASE_URL=http://ollama:11434

CMD ["openclaw", "gateway"]
